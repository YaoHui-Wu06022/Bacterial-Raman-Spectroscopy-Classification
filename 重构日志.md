# 重构日志

## 2026-08-04：迁移 Stanford 预训练与单阶段微调扩展

- 新增 `raman_temp/extensions/stanford_finetune/`，提供 `stanford prepare`、`stanford pretrain` 与 `stanford transfer`。Stanford 不进入常规 profile 映射；扩展单向依赖 core、data、modeling 和 training，核心包不反向导入扩展。
- Stanford 预训练集按固定参考轴进行原生点选择和坏段删除，保持 30 个直属菌株目录，不合并类别前缀、不执行插值、基线处理或 PCA。公开数组下载、导入与 `init.npz` 恢复均位于扩展数据模块。
- 迁移从 Stanford 来源 run 快照取得输入和模型规格，以目标常规 profile 的训练策略构建一次性配置；新的分类头始终重置，只加载非 `head.*` 骨干权重。`trainable_modules` 支持逐个选择，未选模块保持冻结和 eval。
- `TrainRequest` 新增通用 `train_dir`，`data/build.py` 新增经校验的外部参考轴输入；两者默认行为不变，分别供 Stanford 预训练与目标迁移构建使用。
- 验证：合成 Stanford 光谱保留叶子类别和原生值，外部参考轴构建、分类头重置、逐模块冻结、显式训练目录和 Stanford CLI 解析均通过；Python 3.10 CPU 环境完整 `pytest tests -q` 通过 67 项。`zip` 仅完成边界设计，尚未实现命令。

## 2026-08-04：接入训练命令并修缮 analysis 职责

- 新增 `raman_temp/training/cli.py` 与模块入口，规范训练命令为 `train --profile --level`，支持按父类、显式全局模型、固定实验目录、run 名与恢复目录。命令只构造 `TrainRequest`，训练算法与配置字段仍由 workflow 和 `core/config.py` 管理。
- `training/__init__.py` 改为无重依赖导入，根级命令在构建 parser 时不会加载 PyTorch。
- analysis 从单一 runner 拆分为 `context.py`、`integrated_gradients.py`、`layer_attribution.py`、`embedding.py`、`se_summary.py` 和 `report.py`。IG、层归因、Train/Val UMAP、SE 摘要、图表/CSV 和多 parent 聚合各自只保留本身职责，runner 只编排任务和产物目录。
- 验证：训练命令参数解析、训练 workflow、IG、层归因和单 run 分析编排均有测试；Python 3.10 CPU 环境完整 `pytest tests -q` 通过 63 项。新代码未增加项目模块别名导入；训练 CLI 仅在执行时加载 workflow，analysis 只单向依赖 evaluation、data、inference 和下层模型服务。

## 2026-08-04：接入 data 命令层

- 新增 `raman_temp/data/cli.py` 与模块入口，统一提供 `data build train/test`、`data pack/unpack`、`data count`、`data plot`；profile 通过固定映射解析，不增加自定义路径的命令分支。
- 迁移 `count.py` 的目录树统计和 `plot.py` 的训练均值审图。审图按叶子类别、祖先层级和层级概览分别输出，整个 `fig_train/` 在临时目录生成后发布。
- `data pack` 只负责 `init/ → init.npz`；测试菌 `*t` 副本继续仅由 audit 成功提交编排。Python 包导出与 data 命令解耦，待统一包名和导出边界确定后单独实现。
- `data/__init__.py` 改为不做隐式重依赖导入，根级命令帮助不会因注册 data 而加载 PyTorch。
- 验证：临时数据覆盖 pack、unpack、非空目录保护、层级统计、训练图产物与所有 data 命令解析；Python 3.10 CPU 环境完整 `pytest tests -q` 通过 59 项。

## 2026-08-04：迁移模型可解释性 analysis

- 新增 `raman_temp/analysis/`，提供 `analyze interpret run` 与 `analyze interpret parent-routed`；解释性分析不再提供名不副实的 `cascade` 模式。
- `AnalysisConfig` 集中配置 UMAP、IG、归因采样、热图归一化和单子类继承开关；分析任务复用 evaluation 的固定实验、数据索引与 split 上下文。
- 单模型分析输出 Train/Val UMAP、IG 通道重要性、类别波段 CSV 与热图、层归因图和可用的 SE 摘要；parent-routed 按目标层类别空间聚合通道、层、波段和平均光谱。启用继承时，单子类分支映射上一级模型归因。
- 验证：GN 模型的隔离 CPU 冒烟成功生成 UMAP、IG、层归因、热图和 CSV；`pytest tests -q` 通过 56 项。项目内 `import as` 数量为 0。

## 2026-08-03：迁移验证集评估与 PCA-SVM baseline

- 新增 `raman_temp/evaluation/`：`context.py` 统一读取实验快照、层级元数据、固定切分和数据索引；`model_eval.py` 负责模型 `run`、`parent-routed` 与 `cascade` 评估；`baseline.py` 仅拟合 PCA-SVM，不加载深度模型运行时。
- 新规范命令为 `evaluate model run`、`evaluate model parent-routed`、`evaluate model cascade`、`evaluate baseline run` 与 `evaluate baseline parent-routed`。条件父类已知的评估名称统一为 `parent-routed`；baseline 移除不执行逐层预测的 `cascade` 模式。
- PCA-SVM 默认保留 `0.95` 解释方差，默认使用第一强度通道，`--all-channels` 才拼接全部确定性输入通道。父类路由 baseline 直接由层级元数据的父子映射划分任务，单子类分支直接返回唯一类别。
- 评估产物保留既有目录槽位和文件名；模型结果保留逐样本 CSV 与 `used_runs.json`，baseline 保留指标、混淆矩阵和可用的 PCA 散点图。
- 验证：五种评估模式均以 GN 模型和当前数据的隔离小样本在 CPU 冒烟通过，产物齐全；`pytest tests -q` 通过 56 项。静态检查确认 `raman_temp` 无模块循环依赖，项目内 `import as` 数量为 0。该历史 GN run 的固定 split 中有 3068 条训练样本路径不在当前数据目录内，完整评估会明确报错，不自动改写切分。

## 2026-08-03：建立首批统一命令入口

- 新增 `raman_temp/__main__.py` 与 `raman_temp/cli/main.py`，规范入口为 `python -m raman_temp <domain> <command>`；当前接入 `audit` 与 `infer test`，根目录脚本保持不变。
- `audit/cli.py` 与 `inference/cli.py` 均收敛为 `configure_parser()`、`run_command()` 和独立模块 `main()` 三层；参数声明不加载工作流、绘图库或模型。
- `inference/__init__.py` 改为按需暴露公共推理接口，使根级帮助和命令解析不加载 PyTorch。
- 验证：根级、`audit`、`infer test` 与两个领域模块的 `--help` 均可用；Python 3.10 CPU 环境 `pytest tests -q` 通过 56 项。命令帮助不加载 PyTorch；静态检查确认 `raman_temp` 无模块循环依赖，项目内 `import as` 数量为 0。

本日志按大修改记录已完成的重构内容、验证结果和后续影响。设计约束与待迁移范围见 `raman_temp/重构设计.md`。

## 2026-08-03：接入训练 workflow 与微调插件接口

- 新增 `raman_temp/training/workflow.py`，由 `TrainRequest` 统一接收完整配置、可选恢复目录及微调回调。workflow 负责实验目录、切分复用、全局/父类任务枚举、训练/验证数据视图、模型构建、run 编排和元数据汇总；单任务循环继续只负责 epoch 训练。
- `TrainRequest.initialize_model(model, task)` 用于扩展加载预训练权重、替换分类头和设置可训练模块；`apply_training_mode(model)` 用于每个 epoch 维持冻结模块状态。常规 workflow、循环和模型包均不导入 Stanford 扩展。
- `hierarchy_meta.json` 条目保留相对实验根的模型、配置和日志路径，以及 `train_split.json`、`val_split.json` 与 split 哈希；新条目会与已有全局层、父类子模型和 run 历史合并。新 workflow 不生成重复的 `class_names.json`。
- `DatasetIndex` 补齐标准 `__len__()`；层级元数据补齐 `leaf` 的直接父层信息。输入构建在强度数组进入张量边界前复制，保持缓存只读且消除 PyTorch 对不可写数组的警告。
- 验证：全局层与指定父类的 CPU workflow 冒烟、训练循环、层级元数据合并、数据运行时、配置快照等 20 项测试通过。真实 `dataset/alldata/init/` 样本输入形状为 `(2, 1340)`，与基线逐点最大绝对误差为 `0.0`。完整收集结果为 51 项通过；`raman/audit/test_pool.py` 中的辅助函数被 pytest 误收集，缺少 `name` fixture，产生 1 个既有收集错误。

## 2026-08-03：移除训练循环上下文并隔离微调策略

- `raman_temp/training/loop.py` 移除同时携带语义、配置、设备、路径和冻结策略的 `TrainContext`；`run_train_loop()` 现在只接收模型、loader、`TrainTask`、`TrainSpec`、`RuntimeSpec`、`TrainArtifacts` 和显式日志函数。
- `TrainArtifacts` 只保存模型、SE sidecar、checkpoint 与数值诊断路径；设备由 `RuntimeSpec` 解析，局部标签映射由 `TrainTask` 在循环开始时转为设备张量，训练循环不创建目录或写配置快照。
- `apply_training_mode` 为可选回调。常规训练仅调用 `model.train()`；Stanford 微调扩展可在该回调中维持被冻模块的 `eval` 状态，循环不导入扩展模块。
- 训练和验证均消费输入、数值层级标签、样本路径三元组；验证仍保留独立调用时的可选父类 logits 掩码能力。
- 验证：数据运行时、训练规格、验证、循环冒烟、运行上下文与配置快照共 17 项测试通过。依赖与命名检查确认没有残留 `TrainContext`、项目模块 `import as` 或 loop 到 data/modeling/extensions 的反向 import。

## 2026-08-03：收敛训练任务与运行上下文

- 在 `raman_temp/training/split.py` 新增冻结的 `TrainTask`，由 `build_global_train_task()` 和 `build_parent_train_task()` 分别生成全局层任务与父类子模型任务；任务只携带层级语义、可见类别、局部标签映射、切分索引和类别权重，不携带路径、模型或完整配置。单子类父类返回空任务，后续由 workflow 直接记录跳过状态而不创建 run。
- 新增 `raman_temp/core/run_context.py`：`ExperimentContext` 负责实验根共享快照与 split/元数据路径，`RunContext` 负责单 run 的目录、配置快照、固定模型产物路径和日志句柄。恢复训练目录必须位于目标模型槽位；一个 run 只保存 `logs/run.log` 和 `logs/config.txt`。
- `save_resolved_config()` 支持叠加仅属于当前运行的目录字段，使 `resolved_config.yaml` 保留运行时路径而不回写输入配置对象。
- 验证：`tests/test_training_components.py`、`tests/test_run_context.py` 与 `tests/test_config_file.py` 共 11 项通过；`raman_temp/core` 与 `raman_temp/training` 编译通过。依赖检查确认 `core/run_context.py` 不反向导入 data、modeling 或 training，`split.py` 不依赖 modeling 或 run context；新增应用代码没有项目模块 `import as`。

## 2026-08-03：重设训练数据索引与输入视图

- 创建 `raman_temp/data/index.py` 的 `DatasetIndex`，集中训练目录扫描、稳定层级标签、父子映射和一次性只读原始强度缓存；训练和验证数据视图共享同一索引，不再各自扫描目录或维护模块级缓存。
- 收敛 `raman_temp/data/dataset.py` 的 `RamanDataset`：它只按索引构建输入张量，单样本返回输入、数值多层标签和样本路径。
- 将 `InputPreprocessor` 调整为 `preprocess_intensity()`；`.arc_data` 读取继续由 `data/io.py` 提供。
- 验证：数据运行时与基础等价测试 13 项通过；随机抽取 `dataset/alldata/init/Klebsiella/KP04/cell2_Area01_001_shift_cos.arc_data`，确定性输入形状为 `[2, 1340]`，与基线最大绝对误差为 `0.0`；Python 3.10 CPU 环境完整测试集 43 项通过。

## 2026-08-03：拆分训练规格、优化器与数值诊断

- `TrainSpec` 收敛为训练时程、切分设置及 `LoaderSpec`、`OptimizerSpec`、`LossSpec` 三个冻结子规格；`RuntimeSpec` 集中设备选择、确定性、恢复、checkpoint 周期和 AMP。
- 创建 `raman_temp/training/optimizer.py`，集中 `build_loader()`、`build_optimizer()` 和 `build_scheduler()`；学习率仍为 stem `0.6×`、主体 `1.0×`、分类头 `1.1×`。
- 创建 `raman_temp/training/diagnostics.py`，集中首个非有限 batch 的缩进 JSON 摘要写入；训练循环调用该模块，不再保留诊断序列化实现。
- 验证：训练组件与循环冒烟测试 9 项通过；Python 3.10 CPU 环境完整测试集 43 项通过。

## 2026-08-03：收敛层级类别元数据

- 创建 `raman_temp/core/hierarchy_meta.py`，集中层级模型元数据条目的构建及 `hierarchy_meta.json` 的缩进 JSON 读写。
- 类别名称只保留在 `hierarchy_meta.json` 的 `class_names_by_level` 中，新训练工作流不生成重复的 `class_names.json`。
- 验证：模块编译与训练循环冒烟测试 2 项通过。

## 2026-08-03：迁移配置快照文件边界

- 创建 `raman_temp/core/config_file.py`，集中 shared、model、resolved 三类 YAML 快照的 UTF-8 读写和共享字段一致性校验。
- 配置快照模块不依赖 `raman/config_io.py`；tuple 字段写入 YAML 后按列表形式参与一致性比较。
- 验证：配置快照往返测试 1 项通过。

## 2026-08-03：迁移训练基础模块

- 创建 `raman_temp/training/losses.py`、`split.py` 与 `checkpoint.py`，将类别权重、Focal、Align、SupCon、训练切分、局部标签映射和训练状态序列化分离为独立职责。
- `TrainScope` 只记录父类与筛选约束；解析范围时不再回写输入配置。切分索引和局部标签映射由各自函数返回，交由后续 workflow 组合。
- checkpoint 在传入 AMP GradScaler 时保存并恢复其状态，同时保留模型、优化器、scheduler、EMA、随机数与早停状态。
- 新增 `tests/test_training_components.py`，覆盖损失数值和梯度、样本与来源前缀切分、无配置回写的范围过滤，以及 checkpoint/GradScaler 恢复；Python 3.10 CPU 环境完整测试集 35 项通过。

## 2026-08-03：迁移训练验证与层级基础能力

- 创建 `raman_temp/core/hierarchy.py`，集中层级标签选择、父类子类 logits 掩码和允许类别索引解析；训练、验证、评估与推理可共用这组纯张量函数。
- 创建 `raman_temp/common/metrics.py`，集中 accuracy、macro-F1 与 macro recall 计算。
- 创建 `raman_temp/training/se_stats.py` 与 `validation.py`；验证直接消费单层 logits，保留局部标签映射、父类约束、无效标签跳过和 SE 通道缩放 sidecar 统计。
- 新增 `tests/test_training_validation.py`，对照层级掩码和指标，并覆盖验证有效样本过滤与 SE 统计；Python 3.10 CPU 环境完整测试集 38 项通过。

## 2026-08-03：迁移训练规格与循环构件

- 创建 `raman_temp/core/config.py`，从完整配置派生冻结的 `TrainSpec` 与 `RuntimeSpec`；配置保留 `*_use` 字段，规格内部布尔字段统一为 `*_enable`。
- 创建 `raman_temp/training/loop.py` 的基础构件：按训练/验证用途建立 DataLoader，按 stem 0.6×、主体 1.0×、分类头 1.1× 学习率建立 AdamW，并建立余弦退火调度器。
- 扩展 `tests/test_training_components.py`，覆盖规格字段提取和冻结性、DataLoader、优化器分组与调度器；训练基础与验证局部测试共 9 项通过。

## 2026-08-03：迁移训练数据运行时与随机增强

- 创建 `raman_temp/data/augmentation.py`，将训练专用的随机增强集中为 `AugmentationSpec`：归一化前处理分段增益、噪声、轴扰动和基线扰动，归一化后处理平移、展宽和局部衰减；常量按作用分组并添加中文说明。
- 创建 `raman_temp/data/input.py`，以 `InputSpec` 构建原始谱、可选平滑谱和可选 `d1` 通道；不提供 `d2` 通道。创建 `raman_temp/data/dataset.py`，负责层级目录扫描、标签和父子映射、原始强度缓存与按需输入构建。
- 扩展 `InputSpec` 与 `build_input_spec(config)`，使数据集只保存冻结的输入和增强规格，不保留完整配置对象。
- 验证：固定种子下增强结果与参考计算一致，关闭增强时多通道输入逐点一致；从 `dataset/alldata/init/` 随机抽取 `Candida/CPA03/cell5_Area01_000_shift_cos.arc_data`，三通道输入形状均为 `[3, 1340]`，最大绝对误差为 `0.0`；Python 3.10 CPU 环境完整测试集 42 项通过。

## 2026-08-03：迁移完整训练循环

- `TrainArtifacts` 固定保存模型、SE 统计、checkpoint、日志路径和日志函数；由 workflow 创建，训练循环只消费该对象。
- 扩展 `raman_temp/training/loop.py`：完成单模型 epoch 循环，保留 stem/主体/分类头三组学习率、Focal 与 EMA 类别权重、启用时参与的 Align/SupCon、父类 logits 掩码、AMP、梯度裁剪、早停、最佳权重、checkpoint 和首个数值异常 JSONL 摘要。
- 新增 `tests/test_training_loop_smoke.py`，覆盖 CPU 单 epoch 训练、验证、最佳权重保存、checkpoint 写入与成功结束后的清理；Python 3.10 CPU 环境完整测试集 43 项通过。

## 2026-08-03：建立并行重构包

- 创建 `raman_temp/`，作为唯一的功能代码修改位置；现有 `raman/` 保持为行为基线。
- 固定 Stanford 参考波数位置为 `dataset/Stanforddataset/reference_wavenumbers.npy`。
- 建立 `raman_temp/core/paths.py`，统一项目根目录、数据集和参考资源路径解析。

## 2026-08-03：迁移光谱基础算子

- 创建 `raman_temp/spectra/`，包含波数轴、坏波段、滤波、归一化、基线校正、宇宙射线处理和单谱预处理。
- 将光谱数值处理与数据集目录编排、绘图和模型输入组装分离。
- 验证：接口级等价测试与随机 `init` 样本逐点对比通过，最大绝对误差为 `0`。

## 2026-08-03：迁移常规数据处理层

- 创建 `raman_temp/data/`，包含常规 profile、`.arc_data`/`init.npz` I/O、构建配置和 `train/test` 构建流程。
- Stanford 配置不再进入常规 profile 映射。
- `train/test` 构建先在临时目录生成并校验，再发布结果；已有产物保留为备份。
- PCA 开关统一为 `pca_enable`：关闭时不生成 `pca_log.txt`，开启时记录筛除结果。
- 验证：随机真实样本完整 `init → train` 结果逐点一致；PCA 开启时保留样本和日志一致；当前等价测试共 12 项通过。

## 2026-08-03：明确架构修正原则

- 将重构目标明确为降低依赖熵、建立单向依赖和唯一职责，而不局限于复现现有实现。
- 现有包作为现状分析与行为验证依据；发现设计问题时先提出修正方案和验证方式，再决定是否改变业务行为。

## 2026-08-03：迁移审核 Stage1 基础

- 创建 `raman_temp/audit/` 的审核配置、结构化记录、报告写入和 Stage1 原始质量模块。
- Stage1 仅在本次运行的数据池内将 candidate 归档到 `work/delete/stage1/`，不直接修改真实数据。
- 阶段报告保留原始指标、候选清单和运行元数据；审核图不由 Stage 自动生成。
- 验证：随机抽取 `dataset/alldata/raw` 的 3 条谱，质量指标与参考实现一致；临时目录测试确认候选移动不影响真实 `init/`。

## 2026-08-03：迁移测试菌运行池与派生副本构建

- 创建 `raman_temp/audit/test_sync.py`，在审核运行目录中复制常规数据与测试菌，生成带来源清单的联合数据池。
- 创建 `raman_temp/data/test_transfer.py`，按 cell 测量组抽取测试菌谱，构建 `*t` 副本、manifest 和稳定文件夹映射。
- `*t` 构建算法归属 `data/`；`audit` 只负责成功后的调用编排。
- 验证：临时样本确认测试菌仅复制到运行池、未映射目录计数正确；`*t` 副本按单个 cell 组抽样并生成 manifest，且文件选择和两份清单与参考实现一致；当前测试共 17 项通过。

## 2026-08-03：解耦自动平移特殊配置

- 将自动平移的锚点阈值、真菌双峰规则、特殊累计平移和大平移白名单集中到 `AuditConfig`。
- 自动平移后续不再通过读取 `FUNG` 数据集目录决定算法分支。
- 验证：真菌属、特殊文件夹与固定累计平移均与当前配置一致；相关测试 6 项通过。

## 2026-08-03：迁移近邻相似性审核

- 创建审核预处理入口，复用训练一致的光谱预处理和 SNV 标准化，并验证随机原始谱结果一致。
- 创建 Stage2、Stage3 共用的近邻相关性、RMSE 与 MAD 评分内核；移除局部残差面积和重复测量配对评分。
- Stage2 按属和类别前缀分组，Stage3 按多批次类别中的单个文件夹分组；两者只在运行池内移动 candidate。
- 验证：近邻指标和 candidate 判定与参考实现一致；阶段测试确认 CSV 不含已移除字段；当前测试共 21 项通过。

## 2026-08-03：迁移平移内核与 delta 报告

- 创建 1002 cm⁻¹ 锚点代表谱、单峰/双峰拟合、自动平移计划和运行池内文件夹写回。
- 集中 delta 的读写与日志；人工平移使用同一文件夹写回函数，并在测试菌与 `*t` 副本之间校验累计 delta 后同步更新。
- 验证：随机真实文件夹的代表谱、锚点和单文件波数写回与参考实现一致；人工同步与不一致报错测试通过；当前测试共 23 项通过。

## 2026-08-03：收紧审核依赖与命名

- 将宇宙射线参数解析从 `data/build.py` 下沉到 `data/config.py`，使审核预处理不依赖数据构建流程。
- 新增 `audit/io.py` 与 `audit/workspace.py`，分别承载严格原始谱读取和运行池 candidate 移动；Stage1、平移、Stage2、Stage3 不再互相依赖实现细节。
- 统一布尔开关和计算函数命名，保留原有算法与输出字段。
- 验证：完整测试集 23 项通过。

## 2026-08-03：迁移审核提交映射

- `audit/test_sync.py` 根据运行池的阶段归档路径区分主数据与测试菌来源，并在三个 Stage 成功后提供真实 `delete/stage*/` 提交和测试菌保留谱回写。
- 验证：临时双来源样本确认 candidate 不会在测试菌回写时恢复；完整测试集 24 项通过。

## 2026-08-03：接入 audit clean 工作流

- 创建 `audit/workflow.py`，编排运行池、自动平移、Stage1–3、提交计划、candidate 提交、delta、测试菌回写和 `*t` 重建。
- 验证：从 `alldata/raw` 与测试菌 `raw` 抽取样本建立临时数据集，完整 clean 冒烟通过；完整测试集 25 项通过。

## 2026-08-03：强化 audit clean 整种对照

- 将仅复制少量谱的 clean 冒烟测试替换为隔离的完整种对照：复制 `Klebsiella` 全部 424 条原始谱与 `CS01KP` 全部 47 条测试谱，分别运行当前包和 `raman_temp` 的三阶段清洗。
- 验证：两个临时数据集在 `stage1`、`stage2`、`stage3` 的主数据与测试菌 `delete/` 相对路径完全一致；`shift_plan.csv` 的每个平移结果及两侧最终 `delta.txt` 也一致。测试额外确认样本实际产生候选，避免空结果掩盖差异。当前 `tests/test_audit.py` 13 项测试通过。

## 2026-08-03：完成 audit 报告、绘图与命令入口

- `clean` 在成功提交后写入主数据与测试菌的自动平移 `delta_log.txt`；整种对照同步比较日志内容，运行时间字段不参与比较。
- 创建 `audit/plots.py`，提供显式执行的前缀总览图与单文件夹平移前后对照图；`clean` 不调用绘图。
- 创建 `audit/cli.py` 与模块入口，仅提供 `clean`、`plot-prefix`、`plot-shift`、`manual-shift` 四项命令。
- 验证：完整种对照的候选路径、平移计划、delta 与 delta 日志一致；绘图命令产出预期 PNG，命令解析通过。当前 audit 测试 15 项通过。

## 2026-08-03：迁移模型结构与输入规格校验

- 创建 `modeling/spec.py`、`layers.py`、`heads.py`、`classifier.py` 与 `factory.py`，将模型结构字段收敛为 `ModelSpec`，统一经 `build_model()` 构建。
- backbone 仅支持 `cnn` 与完整谱轴 `direct`；保留空 Encoder 用于纯 CNN 对比。模型构建完成后不保留宽泛配置对象或构建期临时状态。
- 创建 `core/input_spec.py` 与 `validate_model_input()`，在模型外部校验输入通道、CNN pooling 后长度和 Transformer 位置编码长度。
- 验证：参考模型的参数键、严格加载、固定输入 logits 与 embedding 一致；GN 实验 checkpoint 严格加载并完成 CPU 前向；CNN、direct/LSTM 与输入规格异常分支通过测试。Python 3.10 CPU 模型测试 4 项通过。
# 配置层边界收敛（2026-08-03）

- 将 `raman_temp/core/config.py` 收敛为唯一可编辑的 Python 配置源：配置按数据集、输入、模型、训练、执行五个不可变数据类分组，移除动态属性代理和运行目录状态。
- 新增 `core/input_spec.py` 的输入规格构建、`training/spec.py` 的训练/执行规格，以及 `core/experiment_reader.py` 的只读 run 快照读取；模型、训练循环和优化器只消费各自的冻结规格。
- 将训练层级、父类筛选、实验目录和 run 名称移入 `TrainRequest`；`RunContext` 只写快照、日志和产物路径，不修改配置。
- 将离线清洗算法参数保留在 `data/config.py`，波数轴和坏段统一由 `InputConfig` 提供；审核配置显式分为输入参数和清洗参数。
- `config_file.py` 只处理原子 YAML 读写、共享字段校验和快照保存；`resolved_config.yaml` 保持扁平可读，路径与任务范围作为元数据保存。

验证：Python 3.10 CPU 环境 `pytest tests -q` 通过 51 项；覆盖审核、数据构建、模型权重严格加载、配置快照与训练循环冒烟。依赖审查确认 `core/config.py` 仅依赖标准库，项目代码未保留扁平 `Config` 访问、`RuntimeSpec` / `TrainSpec` 或项目内 `import as`。
# 独立推理迁移（2026-08-03）

- 新增 `raman_temp/inference/`：`predictor.py` 按 `hierarchy_meta.json` 和每个 run 的快照构建模型并严格加载；`spectra.py` 只负责单谱输入；目录、标签统计、报告和完整编排分别独立。
- 独立推理保留级联、指定单 run、父类子模型、top-k、测试菌前缀评估、迁移样本跳过、逐谱文本、文件夹汇总、光谱图和 `used_runs.json`。
- 输出目录保持 `test_result/` 规则：级联为 `<experiment_dir>/<level>/test_result/`，指定 run 为 `<run_dir>/test_result/`；发布在完整临时产物生成后进行，已有结果保留为同级副本。
- `InputSpec` 的点数调整为坏段筛除后实际输入长度，导数步长仍使用完整统一波数轴步长；这与训练数据和独立测试谱的 851 点输入一致。

验证：使用 `output/GN/20260802_231209_88%` 的 `level_1` 模型，在 CPU 上对 `dataset/GN/test/CS01KP` 全部 47 条谱逐条比较，新包与基准包 top-3 标签和概率一致。`pytest tests -q` 通过 54 项。
